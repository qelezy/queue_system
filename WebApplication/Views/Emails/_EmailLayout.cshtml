@model WebApplication.Models.Emails.IEmailPageViewModel
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>@Model.Title</title>
    <style>
        html, body {
            margin: 0;
            padding: 0;
            background: #ffffff;
            font-family: 'Poppins', Arial, Helvetica, sans-serif;
            color: #333333;
            -webkit-text-size-adjust: 100%;
        }
        body {
            text-align: center;
        }
        .email-outer {
            width: 100%;
            border-collapse: collapse;
        }
        .email-outer__cell {
            padding: 24px 16px;
            text-align: center;
        }
        .email-card-shell {
            display: inline-block;
            width: 100%;
            max-width: 600px;
            border-radius: 16px;
            border: 1px solid rgba(0, 179, 184, 0.12);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            text-align: left;
        }
        .email-card {
            width: 100%;
            border-collapse: collapse;
            border-spacing: 0;
        }
        .email-header {
            background-color: #00b3b8;
            padding: 28px 32px;
        }
        .email-header__title {
            margin: 0;
            font-size: 26px;
            font-weight: 600;
            line-height: 1.3;
            color: #ffffff;
            text-align: left;
        }
        .email-content {
            padding: 36px 32px;
            background: #ffffff;
        }
        .email-body {
            font-size: 16px;
            line-height: 1.55;
            color: #333333;
            text-align: left;
        }
        .email-body p {
            margin: 0 0 16px;
        }
        .email-body p:last-child {
            margin-bottom: 0;
        }
        .email-password-row {
            text-align: center;
            margin: 0 0 16px;
        }
        .email-password {
            display: inline-block;
            padding: 10px 16px;
            margin: 0;
            background: rgba(0, 179, 184, 0.12);
            border-radius: 6px;
            font-weight: 600;
            font-size: 22px;
            color: #333333;
            word-break: break-all;
        }
        .email-hint {
            margin: 0 0 16px;
            padding: 12px 16px;
            background: #fff8e8;
            border-left: 4px solid #d4a017;
            border-radius: 6px;
            font-size: 15px;
            line-height: 1.5;
            color: #6b5420;
        }
        .email-btn-wrap {
            margin: 24px 0 0;
            text-align: center;
        }
        .email-btn {
            display: block;
            width: 100%;
            box-sizing: border-box;
            padding: 14px 28px;
            font-size: 17px;
            font-weight: 500;
            color: #ffffff !important;
            background-color: #00b3b8;
            border-radius: 6px;
            text-decoration: none;
            text-align: center;
        }
    </style>
</head>
<body>
    <table class="email-outer" role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%">
        <tr>
            <td class="email-outer__cell" align="center">
                <div class="email-card-shell">
                    <table class="email-card" role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%">
                        <tr>
                            <td class="email-header">
                                <h1 class="email-header__title">@Model.Title</h1>
                            </td>
                        </tr>
                        <tr>
                            <td class="email-content">
                                <div class="email-body">
                                    @RenderBody()
                                </div>
                            </td>
                        </tr>
                    </table>
                </div>
            </td>
        </tr>
    </table>
</body>
</html>
